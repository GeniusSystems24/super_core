# SuperCore Package — Version 1.3.0 Update Requirements

Update the Flutter package located at `flutter/super_core` to version `1.3.0`.

## ThemeData

Complete the configuration of all remaining `ThemeData` properties that are not explicitly provided through the theme builder.

Generate appropriate default values for these properties in accordance with the GeniusLink Design System, while preserving any values explicitly supplied by the builder.

## ColorScheme

Define appropriate values for the following remaining `ColorScheme` properties:

* `primaryFixed`
* `primaryFixedDim`
* `onPrimaryFixed`
* `onPrimaryFixedVariant`
* `secondaryFixed`
* `secondaryFixedDim`
* `onSecondaryFixed`
* `onSecondaryFixedVariant`
* `tertiaryFixed`
* `tertiaryFixedDim`
* `onTertiaryFixed`
* `onTertiaryFixedVariant`
* `surfaceDim`
* `surfaceBright`
* `surfaceContainerLowest`
* `surfaceContainerLow`
* `surfaceContainer`
* `surfaceContainerHigh`
* `surfaceContainerHighest`

The generated colors must be visually consistent, accessible, and aligned with the GeniusLink Design System in both light and dark themes.

## Scaffold Theme

Set the Scaffold background color to the `surface` color defined in the active `ColorScheme`.

The surrounding surface and container colors should provide sufficient visual separation so that cards, panels, fields, and other interface elements remain clearly distinguishable from the Scaffold background.

## AppBar Theme

Configure the `AppBar` background color so that it is visually distinct from the Scaffold background while remaining consistent with the active theme and the GeniusLink Design System.

Configure the system UI so that:

* The Status Bar background color matches the `AppBar` background color.
* The Navigation Bar background color matches the `AppBar` background color.
* Status Bar and Navigation Bar icon brightness is selected automatically to maintain sufficient contrast and readability.

Apply the appropriate system overlay styles for both light and dark themes.

## Documentation

* Update the package documentation according to pub.dev documentation standards and Dart package best practices.
* Update `README.md` to document the new theme behavior, supported properties, configuration options, and usage examples.
* Update `CHANGELOG.md` with all changes introduced in version `1.3.0`.
* Update the example application screen to demonstrate the updated `ThemeData`, `ColorScheme`, Scaffold, AppBar, Status Bar, and Navigation Bar behavior.
* Ensure all public APIs include clear and accurate Dart documentation comments.
* Update ai skill
  * `skill\chatgpt_codex`
  * `skill\claude_code`
